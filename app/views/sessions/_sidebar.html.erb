<div class="sidebar sidebar__admin">
    <div class="menu">
        <%= link_to control_panel_path(key: "headers") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "heading")%></div>
                    <div class="menu__title"><%= Header.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "posts") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "newspaper")%></div>
                    <div class="menu__title"><%= Post.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "banners") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "image")%></div>
                    <div class="menu__title"><%= Banner.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "leaders") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "user")%></div>
                    <div class="menu__title"><%= Leader.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "abouts") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "table-list")%></div>
                    <div class="menu__title"><%= About.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "sciences") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "atom")%></div>
                    <div class="menu__title"><%= Science.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "products") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "box")%></div>
                    <div class="menu__title"><%= Product.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "careers") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "id-card")%></div>
                    <div class="menu__title"><%= Career.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "others") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "globe")%></div>
                    <div class="menu__title"><%= Other.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "documents") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "file")%></div>
                    <div class="menu__title"><%= Document.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "themes") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "list-ul")%></div>
                    <div class="menu__title"><%= Theme.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
        <%= link_to control_panel_path(key: "activities") do %>
            <div class="menu__item">
                <div class="menu__body">
                    <div class="menu__icon"><%= icon("fa-solid", "briefcase")%></div>
                    <div class="menu__title"><%= Activity.model_name.human(count: :many) %></div>
                </div>
            </div>
        <% end %>
    </div>
    <%= link_to logout_path, method: :delete, class: "button__link", style: "width: 100%" do %>
        <div class="button button_primary" style="width: 100%">Выход</div>
    <% end %>
</div>
