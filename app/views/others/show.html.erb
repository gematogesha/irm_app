<%- title @page_title_text %>

<section class="section-show section_gray">
    <div class="wrapper flex_stretch_start wrapper__content">
        <%= render "shared/sidebar", controller: @others %>
        <div class="other_show-list">
            <div class="content-wrapper other_show-content">
                <%= @other.content %>
            </div>
        </div>
    </div>
</section>
 