<%- title @page_title_text %>

<section class="section-posts section_gray">
    <div class="wrapper">
        <div class="post">
            <%= turbo_frame_tag "posts", src: posts_get_posts_path, loading: :lazy, target: "_top" do %>
                <%= render "post_sceleton", line_ind: @line_ind %>
            <% end %>
        </div>
    </div>
</section>
