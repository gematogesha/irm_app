<%- title @page_title_text %>

<section>
    <div class="wrapper">
        <% @posts.each_with_index do |post, index| %>
            <%= link_to post.title, post_path(post.number) %>
            <br>
        <% end %>
    </div>
</section>
